

Things todo:

    Refactoring:
        *   Seperate database logic from accountt class
        *   Account::authenticate() method: take away and seprat things that should not be there

    Logical:
        (Account Class)
        *  Account ID as constant is some way once initialized (write later, once deaclred, never changes)
        *  define other functions like checkisAdmin(0 and updateAccessState()
